services:
  postgres:
    image: postgres:16-alpine
    container_name: way-education-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: way_education
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d way_education"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
